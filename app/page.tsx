import HomeClient from "../components/HomeClient";

export const metadata = {
  title: "Advance Fortify | Websites that convert",
  description:
    "We build fast, SEO-ready websites and landing pages designed to turn visitors into clients — clean, modern, and conversion-focused.",
};

export default function HomePage() {
  return <HomeClient />;
}
